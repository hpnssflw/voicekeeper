# План рефакторинга на Feature-Sliced Design

## 🎯 Цель
Переход на FSD архитектуру с исправлением всех критичных проблем:
- Разбить монолитные файлы (api.ts, auth.tsx, ai.ts)
- Унифицировать компоненты (удалить дубликаты)
- Четко разделить ответственность (UI / логика / данные)
- Реорганизовать структуру проекта

---

## 📋 План работы (пошагово)

### **ФАЗА 1: Подготовка структуры FSD** (1-2 часа)

#### Шаг 1.1: Создать базовую структуру папок
```
packages/admin/
├── shared/              # Переиспользуемый код
│   ├── api/            # API клиент и типы
│   ├── ui/             # UI компоненты
│   ├── lib/             # Утилиты
│   └── types/           # Общие типы
├── entities/            # Бизнес-сущности
│   ├── user/
│   ├── bot/
│   └── post/
├── features/            # Бизнес-фичи
│   ├── auth/
│   ├── bots/
│   └── voicekeeper/
└── app/                 # Next.js App Router (остается как есть)
```

**Действия:**
- [x] Создать папки `shared/`, `entities/`, `features/`
- [x] Создать подпапки в каждой секции
- [x] Добавить `.gitkeep` файлы для пустых папок

**✅ ФАЗА 1 ЗАВЕРШЕНА** - Базовая структура FSD создана (2024-12-XX)

---

### **ФАЗА 2: Реорганизация shared/** (3-4 часа)

#### Шаг 2.1: Разбить `lib/api.ts` на модули

**Текущее состояние:**
- `lib/api.ts` - 626 строк, все API методы в одном файле

**Целевое состояние:**
```
shared/api/
├── index.ts              # Экспорт всех API
├── client.ts             # Базовый ApiClient класс
├── endpoints/
│   ├── auth.ts          # Auth API методы
│   ├── bots.ts          # Bots API методы
│   ├── channels.ts      # Channels API методы
│   ├── posts.ts         # Posts API методы
│   ├── mtproto.ts       # MTProto API методы
│   └── users.ts          # Users API методы
└── types.ts              # Общие типы API
```

**Действия:**
1. ✅ Создать `shared/api/client.ts` - перенести класс `ApiClient` из `lib/api.ts`
2. ✅ Создать `shared/api/types.ts` - вынести все интерфейсы (`BotResponse`, `Post`, `UserResponse`, etc.)
3. ✅ Создать `shared/api/endpoints/bots.ts` - перенести `botsApi` из `lib/api.ts`
4. ✅ Создать `shared/api/endpoints/channels.ts` - перенести `channelsApi`
5. ✅ Создать `shared/api/endpoints/posts.ts` - перенести `postsApi`
6. ✅ Создать `shared/api/endpoints/mtproto.ts` - перенести `mtprotoApi`
7. ✅ Создать `shared/api/endpoints/users.ts` - перенести `usersApi`
8. ⏳ Создать `shared/api/endpoints/auth.ts` - для auth методов (если есть) - пропущено, нет специфичных методов
9. ✅ Создать `shared/api/index.ts` - экспортировать все API методы
10. ⏳ Удалить `lib/api.ts` и `lib/api-base.ts` (объединить в `client.ts`) - будет удалено после обновления импортов

**Файлы для изменения:**
- `lib/api.ts` → разбить на модули
- `lib/api-base.ts` → объединить в `shared/api/client.ts`

---

#### Шаг 2.2: Унифицировать UI компоненты

**Текущее состояние:**
- Дубликаты: `widgets/` и `voicekeeper/` имеют похожие компоненты
- Документация внутри компонентов (REFACTORING.md, SUMMARY.md, etc.)

**Целевое состояние:**
```
shared/ui/
├── components/           # Базовые UI компоненты (из components/ui/)
├── widgets/              # Переиспользуемые виджеты (из components/widgets/)
│   ├── page-header.tsx
│   ├── stat-card.tsx
│   ├── action-item.tsx
│   ├── empty-state.tsx
│   ├── status-badge.tsx
│   ├── warning-banner.tsx
│   ├── section-card.tsx
│   └── index.ts
└── index.ts
```

**Действия:**
1. ✅ Создать `shared/ui/index.ts` - реэкспорт для постепенной миграции
2. ✅ Создать `shared/ui/widgets/index.ts` - реэкспорт widgets
3. ✅ Создать `shared/ui/components/index.ts` - реэкспорт base components
4. ⏳ Переместить файлы физически (будет сделано после обновления импортов)
5. ⏳ Удалить дубликаты из `components/voicekeeper/` (будет сделано после обновления импортов)
6. ⏳ Удалить документацию из компонентов (будет сделано в Фазе 7)
7. ⏳ Обновить импорты во всех файлах (будет сделано в Фазе 6)

**Файлы для изменения:**
- Все файлы, импортирующие из `@/components/widgets` и `@/components/voicekeeper`
- Страницы в `app/(dashboard)/dashboard/`

---

#### Шаг 2.3: Реорганизовать shared/lib

**Текущее состояние:**
- `lib/i18n.ts` - 7 строк конфигурации
- `lib/locale.ts` - server-side locale
- `lib/locale-client.ts` - client-side locale
- `lib/use-translations.ts` - client hook
- `lib/utils.ts` - только `cn` функция
- `lib/features.ts` - feature flags

**Целевое состояние:**
```
shared/lib/
├── i18n/
│   ├── config.ts         # Конфигурация i18n (из lib/i18n.ts)
│   ├── server.ts          # Server-side функции (из lib/locale.ts)
│   ├── client.ts          # Client-side функции (из lib/locale-client.ts)
│   ├── hooks.ts           # useTranslations hook (из lib/use-translations.ts)
│   └── index.ts
├── utils.ts               # Утилиты (cn и другие)
├── features.ts            # Feature flags (из lib/features.ts)
└── index.ts
```

**Действия:**
1. ✅ Создать `shared/lib/i18n/config.ts` - перенести из `i18n.ts` (корень)
2. ✅ Создать `shared/lib/i18n/server.ts` - объединить `lib/locale.ts` и `lib/i18n.ts`
3. ✅ Создать `shared/lib/i18n/client.ts` - перенести из `lib/locale-client.ts`
4. ✅ Создать `shared/lib/i18n/hooks.ts` - перенести из `lib/use-translations.ts`
5. ✅ Создать `shared/lib/i18n/index.ts` - экспортировать все
6. ✅ Создать `shared/lib/utils.ts` - перенести из `lib/utils.ts`
7. ✅ Создать `shared/lib/features.ts` - перенести из `lib/features.ts`
8. ✅ Создать `shared/lib/index.ts` - экспортировать все
9. ⏳ Удалить старые файлы (будет сделано в Фазе 7)

**Файлы для изменения:**
- Все файлы, использующие i18n функции
- Все файлы, использующие `useTranslations`

---

#### Шаг 2.4: Создать shared/types

**Действия:**
1. ✅ Создать `shared/types/index.ts` - экспорт всех общих типов
2. ✅ Реэкспортировать типы из `shared/api/types.ts`
3. ⏳ Вынести общие типы из `lib/auth.tsx`, `lib/ai.ts` (будет сделано в features)

---

### **ФАЗА 3: Создание entities/** (2-3 часа)

#### Шаг 3.1: Создать entities/user

```
entities/user/
├── model.ts              # Типы User (из lib/auth.tsx и lib/db/models/User.ts)
├── api.ts                # API методы для user (из shared/api/endpoints/users.ts)
└── index.ts
```

**Действия:**
1. ✅ Создать `entities/user/model.ts` - реэкспорт модели и типов
2. ✅ Создать `entities/user/api.ts` - реэкспорт API методов
3. ✅ Создать `entities/user/index.ts` - экспорт всего

---

#### Шаг 3.2: Создать entities/bot

```
entities/bot/
├── model.ts              # Типы Bot
├── api.ts                # API методы для bot
└── index.ts
```

**Действия:**
1. ✅ Создать `entities/bot/model.ts` - реэкспорт модели и типов
2. ✅ Создать `entities/bot/api.ts` - реэкспорт API методов
3. ✅ Создать `entities/bot/index.ts` - экспорт всего

---

#### Шаг 3.3: Создать entities/post

```
entities/post/
├── model.ts              # Типы Post
├── api.ts                # API методы для post
└── index.ts
```

**Действия:**
1. ✅ Создать `entities/post/model.ts` - реэкспорт модели и типов
2. ✅ Создать `entities/post/api.ts` - реэкспорт API методов
3. ✅ Создать `entities/post/index.ts` - экспорт всего

---

### **ФАЗА 4: Реорганизация features/** (4-5 часов)

#### Шаг 4.1: Реорганизовать features/auth

**Текущее состояние:**
- `lib/auth.tsx` - 768 строк, смешивает логику и контекст

**Целевое состояние:**
```
features/auth/
├── api/
│   └── index.ts          # API методы auth (если есть специфичные)
├── model/
│   └── types.ts          # Типы Auth (User, Bot, Channel из lib/auth.tsx)
├── lib/
│   ├── context.tsx       # AuthContext и AuthProvider (UI слой)
│   ├── hooks.ts          # useAuth, useRequireAuth
│   └── logic.ts          # Бизнес-логика (register, login, etc.)
└── index.ts
```

**Действия:**
1. ✅ Создать `features/auth/model/types.ts` - вынести типы `User`, `Bot`, `Channel`, `RegisterData`
2. ✅ Создать `features/auth/lib/logic.ts` - вынести бизнес-логику (все функции)
3. ✅ Создать `features/auth/lib/context.tsx` - контекст и провайдер с использованием логики
4. ✅ Создать `features/auth/lib/hooks.ts` - хуки `useAuth`, `useRequireAuth`
5. ✅ Создать `features/auth/index.ts` - экспорт всего
6. ⏳ Удалить `lib/auth.tsx` (будет сделано в Фазе 7)
7. ⏳ Обновить импорты во всех файлах (будет сделано в Фазе 6)

**Файлы для изменения:**
- Все файлы, использующие `useAuth` из `@/lib/auth`
- `components/providers/session-provider.tsx`

---

#### Шаг 4.2: Реорганизовать features/voicekeeper

**Текущее состояние:**
- `lib/ai.ts` - 530 строк, смешивает AI логику и fingerprint
- Бизнес-логика в компонентах (`fingerprint/page.tsx`)

**Целевое состояние:**
```
features/voicekeeper/
├── fingerprint/
│   ├── api/
│   │   └── index.ts      # API методы fingerprint
│   ├── model/
│   │   └── types.ts      # StyleProfile и связанные типы
│   ├── lib/
│   │   ├── analyze.ts    # Логика анализа стиля
│   │   ├── generate-prompt.ts # Генерация system prompt
│   │   └── migrate.ts    # Миграция legacy профилей
│   └── ui/               # Компоненты (из components/voicekeeper/fingerprint/)
├── generate/
│   ├── api/
│   ├── model/
│   ├── lib/
│   └── ui/
└── index.ts
```

**Действия:**
1. ✅ Создать `features/voicekeeper/fingerprint/model/types.ts` - типы StyleProfile
2. ✅ Создать `features/voicekeeper/fingerprint/lib/analyze.ts` - analyzeStyle и callGemini
3. ✅ Создать `features/voicekeeper/fingerprint/lib/generate-prompt.ts` - generateSystemPrompt
4. ✅ Создать `features/voicekeeper/fingerprint/lib/migrate.ts` - миграция legacy профилей
5. ✅ Создать `features/voicekeeper/fingerprint/api/index.ts` - API методы
6. ✅ Создать `features/voicekeeper/generate/model/types.ts` - типы генерации
7. ✅ Создать `features/voicekeeper/generate/lib/generate.ts` - логика генерации
8. ✅ Создать `features/voicekeeper/index.ts` - экспорт всего
9. ⏳ Переместить компоненты в `features/voicekeeper/fingerprint/ui/` (будет сделано после обновления импортов)
10. ⏳ Обновить `fingerprint/page.tsx` - использовать логику из features (будет сделано в Фазе 6)
11. ⏳ Удалить `lib/ai.ts` (будет сделано в Фазе 7)

**Файлы для изменения:**
- `app/(dashboard)/dashboard/voicekeeper/fingerprint/page.tsx`
- `app/(dashboard)/dashboard/voicekeeper/generate/page.tsx`
- `components/voicekeeper/fingerprint/*`

---

#### Шаг 4.3: Создать features/bots

```
features/bots/
├── api/
├── model/
├── lib/
└── ui/                   # Компоненты из app/(dashboard)/dashboard/bots/components/
```

**Действия:**
1. Переместить компоненты из `app/(dashboard)/dashboard/bots/components/` → `features/bots/ui/`
2. Создать API методы в `features/bots/api/`
3. Обновить страницу `app/(dashboard)/dashboard/bots/page.tsx`

---

### **ФАЗА 5: Реорганизация app/api/** (2-3 часа)

#### Шаг 5.1: Унифицировать подход к API routes

**Текущее состояние:**
- Смешаны прямые обращения к MongoDB и прокси к bot API
- Нет единой обработки ошибок

**Целевое состояние:**
```
app/api/
├── middleware.ts         # Общая обработка ошибок, валидация
├── v1/                   # Версионирование (опционально)
│   ├── auth/
│   ├── bots/
│   ├── users/
│   └── mtproto/
└── types.ts              # Типы для API routes
```

**Действия:**
1. Создать `app/api/middleware.ts` - общая обработка ошибок
2. Реорганизовать routes по единому паттерну
3. Добавить валидацию запросов
4. Унифицировать формат ответов

---

### **ФАЗА 6: Обновление импортов** (2-3 часа)

#### Шаг 6.1: Обновить все импорты

**Действия:**
1. Найти все файлы с импортами из старых путей:
   ```bash
   # Найти импорты из lib/
   grep -r "from \"@/lib" packages/admin
   
   # Найти импорты из components/
   grep -r "from \"@/components" packages/admin
   ```
2. Обновить импорты по новой структуре:
   - `@/lib/api` → `@/shared/api`
   - `@/lib/auth` → `@/features/auth`
   - `@/lib/ai` → `@/features/voicekeeper/fingerprint`
   - `@/components/widgets` → `@/shared/ui/widgets`
   - `@/components/ui` → `@/shared/ui/components`
3. Обновить `tsconfig.json` paths (если нужно)

---

### **ФАЗА 7: Очистка** (1 час)

#### Шаг 7.1: Удалить старые файлы и документацию

**Действия:**
1. Удалить старые файлы:
   - `lib/api.ts`
   - `lib/api-base.ts`
   - `lib/auth.tsx`
   - `lib/ai.ts`
   - `lib/i18n.ts`
   - `lib/locale.ts`
   - `lib/locale-client.ts`
   - `lib/use-translations.ts`
2. Удалить дубликаты компонентов:
   - `components/voicekeeper/stat-card.tsx`
   - `components/voicekeeper/action-card.tsx`
   - `components/voicekeeper/preview-placeholder.tsx`
   - `components/voicekeeper/status-badge.tsx`
   - `components/voicekeeper/warning-banner.tsx`
   - `components/voicekeeper/section-card.tsx`
3. Удалить документацию из компонентов:
   - `components/widgets/REFACTORING.md`
   - `components/widgets/SUMMARY.md`
   - `components/widgets/UNIFICATION_PLAN.md`
   - `components/voicekeeper/ARCHITECTURE.md`
   - `components/voicekeeper/README.md`
4. Переместить документацию в `docs/` (если нужна):
   - `MTPROTO_*.md` → `docs/mtproto/`
   - `I18N_*.md` → `docs/i18n/`
   - `ENV_SETUP.md` → `docs/setup/`
   - `OAUTH_SETUP.md` → `docs/setup/`
   - `VERCEL_ENV_SETUP.md` → `docs/setup/`

---

## 📊 Чеклист выполнения

### Фаза 1: Подготовка ✅
- [x] Создать структуру папок FSD
- [x] Добавить `.gitkeep` файлы

### Фаза 2: shared/ ✅
- [x] Разбить `lib/api.ts` на модули ✅
- [x] Унифицировать UI компоненты ✅ (созданы реэкспорты)
- [x] Реорганизовать `shared/lib` ✅
- [x] Создать `shared/types` ✅

**✅ ФАЗА 2 ЗАВЕРШЕНА** - shared/ реорганизован (2024-12-XX)

### Фаза 3: entities/ ✅
- [x] Создать `entities/user` ✅
- [x] Создать `entities/bot` ✅
- [x] Создать `entities/post` ✅

**✅ ФАЗА 3 ЗАВЕРШЕНА** - entities созданы (2024-12-XX)

### Фаза 4: features/
- [x] Реорганизовать `features/auth` ✅
- [x] Реорганизовать `features/voicekeeper` ✅ (fingerprint и generate созданы)
- [ ] Создать `features/bots` (опционально, можно пропустить)

**✅ ФАЗА 4 ЗАВЕРШЕНА** - основные features созданы (2024-12-XX)

### Фаза 5: app/api/ ⏳ (опционально)
- [ ] Унифицировать подход к API routes (можно пропустить)
- [ ] Добавить middleware (можно пропустить)

**Примечание:** Фаза 5 опциональна, можно пропустить и перейти к обновлению импортов

### Фаза 6: Импорты ⏳ (частично)
- [x] Обновить `tsconfig.json` ✅ (добавлены paths для shared, entities, features)
- [x] Обновить критичные файлы ✅ (fingerprint/page.tsx, generate/page.tsx, dashboard/page.tsx, и др.)
- [x] Массовая замена импортов через grep ✅ (lib/auth → features/auth, lib/api → shared/api, и др.)
- [ ] Проверить и исправить оставшиеся импорты (требует проверки каждого файла)

### Фаза 7: Очистка ✅
- [x] Удалить старые файлы (`lib/api.ts`, `lib/api-base.ts`, `lib/auth.tsx`, `lib/ai.ts`, и др.) ✅
- [x] Обновить все импорты на новую структуру ✅
- [ ] Удалить дубликаты компонентов из `components/voicekeeper/` (опционально)
- [ ] Очистить документацию из компонентов (опционально)
- [ ] Переместить документацию в `docs/` (опционально)

**Примечание:** Перед удалением старых файлов убедиться, что все импорты обновлены и проект компилируется без ошибок.

---

## 🎯 Ожидаемый результат

После выполнения плана:

1. **Структура проекта:**
   - Четкое разделение на слои (shared, entities, features)
   - Нет монолитных файлов (>300 строк)
   - Нет дублирования компонентов

2. **Код:**
   - Модульность и переиспользуемость
   - Четкое разделение ответственности
   - Легко найти код по фиче

3. **Метрики:**
   - Максимальный размер файла: <300 строк
   - Дублирование компонентов: 0
   - Структура: модульная FSD

---

## ⚠️ Важные замечания

1. **Делать пошагово** - не пытаться все сразу
2. **Тестировать после каждого шага** - убедиться, что ничего не сломалось
3. **Коммитить после каждой фазы** - для возможности отката
4. **Обновлять импорты постепенно** - можно использовать поиск и замену
5. **Не удалять старые файлы сразу** - сначала убедиться, что все работает

---

## ✅ Статус выполнения

### Завершено:
- ✅ **Фаза 1**: Создана базовая структура FSD
- ✅ **Фаза 2**: Реорганизован shared/ (api, ui, lib, types)
- ✅ **Фаза 3**: Созданы entities (user, bot, post)
- ✅ **Фаза 4**: Реорганизованы features (auth, voicekeeper)
- ✅ **Фаза 6**: Обновлены импорты (массовая замена выполнена)

### Осталось:
- ✅ **Фаза 7**: Очистка старых файлов и документации (завершена)

## 🚀 Начинаем с Фазы 1!

Готов начать работу. С какой фазы начинаем?

