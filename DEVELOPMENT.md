# VoiceKeeper - Локальный запуск и тестирование

Полное руководство по запуску системы локально для разработки и тестирования.

## 📦 Структура проекта

```
telegram-voronka/
├── packages/
│   ├── admin/          # Next.js админ-панель (порт 3001)
│   ├── bot/            # Express API + Telegram Bot (порт 8080)
│   └── webapp/         # Telegram Mini App (порт 3000)
├── infra/
│   └── docker-compose.yml  # MongoDB + Redis
└── package.json        # Корневой package.json (npm workspaces)
```

## 🔧 Предварительные требования

- **Node.js** 18+ (рекомендуется 20+)
- **Docker** и **Docker Compose** (для MongoDB и Redis)
- **Telegram Bot Token** (от @BotFather)
- **Telegram API Credentials** (опционально, для парсинга каналов)

---

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
git clone https://github.com/hpnssflw/voicekeeper.git
cd telegram-voronka

# Установка всех зависимостей (npm workspaces)
npm install
```

### 2. Запуск инфраструктуры (MongoDB + Redis)

```bash
cd infra
docker-compose up -d
```

Это запустит:
- **MongoDB** на `localhost:27017`
- **Redis** на `localhost:6379`

Проверка:
```bash
docker-compose ps
```

### 3. Настройка переменных окружения

#### Bot (`packages/bot/.env`)

```bash
cd packages/bot
cp .env.example .env  # или создайте вручную
```

Содержимое `.env`:

```env
# === Основные ===
NODE_ENV=development
PORT=8080

# === База данных ===
MONGO_URI=mongodb://localhost:27017/voicekeeper
REDIS_URL=redis://localhost:6379

# === Безопасность ===
JWT_SECRET=your_super_secret_key_change_in_production

# === Telegram Bot ===
# Получите у @BotFather: /newbot
TELEGRAM_BOT_TOKEN=7123456789:AAH_your_bot_token_here

# === Публикация ===
# Канал для публикации постов (бот должен быть админом)
TELEGRAM_CHANNEL_ID=@your_channel
PUBLISH_MODE=channel

# === MTProto (для парсинга каналов) ===
# Получите на https://my.telegram.org/apps
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef

# === Опционально ===
ENABLE_POLLING=true  # Для локальной разработки без webhook
WEBAPP_URL=http://localhost:3000
```

#### Admin (`packages/admin/.env.local`)

```bash
cd packages/admin
```

Создайте файл `.env.local`:

```env
# MongoDB Connection (required for admin API routes)
MONGO_URI=mongodb://localhost:27017/voicekeeper
# Or use MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/voronka?retryWrites=true&w=majority

# API Backend URL (for bot operations - bots, posts, channels)
NEXT_PUBLIC_API_BASE=http://localhost:4000/api
```

### 4. Запуск сервисов

Откройте **3 терминала**:

**Терминал 1 - Bot API:**
```bash
cd packages/bot
npm run dev
```
Ожидаемый вывод:
```
🚀 Server running on port 8080
📦 MongoDB connected
📡 Redis connected
🤖 Bot polling started (dev mode)
```

**Терминал 2 - Admin Panel:**
```bash
cd packages/admin
npm run dev
```
Ожидаемый вывод:
```
▲ Next.js 16.x
- Local: http://localhost:3001
```

**Терминал 3 - Webapp (опционально):**
```bash
cd packages/webapp
npm run dev
```
Ожидаемый вывод:
```
▲ Next.js 16.x
- Local: http://localhost:3000
```

---

## 🧪 Тестирование флоу

### Флоу 1: Регистрация и онбординг

1. Откройте http://localhost:3001
2. Нажмите "Начать" на лендинге
3. Заполните форму регистрации:
   - Email: `test@example.com`
   - Имя: `Test User`
   - Пароль: `password123`
4. На онбординге добавьте токен бота:
   - Вставьте ваш `TELEGRAM_BOT_TOKEN`
5. Выберите план и завершите онбординг
6. Вы попадёте в Dashboard

### Флоу 2: Добавление бота

1. Перейдите в **Боты** (`/dashboard/bots`)
2. Нажмите "Добавить бота"
3. Вставьте токен бота
4. Система проверит токен через Telegram API
5. Бот появится в списке с информацией:
   - Username
   - Telegram ID
   - Статистика

### Флоу 3: Настройка канала для публикации

1. В карточке бота нажмите стрелку для раскрытия
2. В поле "Канал для публикаций" введите:
   - `@your_channel` или
   - `-100123456789` (ID канала)
3. Нажмите "Настроить"
4. ⚠️ **Важно:** Бот должен быть админом канала!

Как добавить бота в канал:
1. Откройте канал в Telegram
2. Настройки → Администраторы
3. Добавьте бота
4. Дайте права на публикацию

### Флоу 4: Создание и публикация поста

1. Перейдите в **Посты** (`/dashboard/posts`)
2. Нажмите "Новый пост"
3. Заполните:
   - Заголовок (опционально)
   - Текст поста
4. Выберите куда публиковать:
   - В канал
   - Подписчикам бота
5. Нажмите "Опубликовать"
6. Пост отправится в канал!

### Флоу 5: Парсинг каналов (MTProto)

1. Убедитесь что `TELEGRAM_API_ID` и `TELEGRAM_API_HASH` настроены
2. Перейдите в **Парсинг каналов** (`/dashboard/channels`)
3. Нажмите "Авторизоваться"
4. Введите номер телефона (ваш личный Telegram)
5. Введите код из Telegram
6. Если есть 2FA - введите пароль
7. Добавьте канал для парсинга: `@channel_name`
8. Нажмите "Парсить"
9. Система загрузит посты канала с:
   - Текстом
   - Просмотрами
   - Хештегами
   - Медиа

---

## 🐛 Отладка

### Логи сервисов

**Bot API логи:**
```bash
# В терминале где запущен bot
# Или через Docker
docker-compose logs -f bot
```

**Проверка MongoDB:**
```bash
# Подключение к MongoDB
docker exec -it infra-mongo-1 mongosh
use voicekeeper
db.bots.find()
db.posts.find()
```

**Проверка Redis:**
```bash
docker exec -it infra-redis-1 redis-cli
KEYS *
```

### Частые проблемы

#### Bot не отвечает
```bash
# Проверьте токен
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"
```

#### MongoDB connection error
```bash
# Проверьте что контейнер запущен
docker-compose ps
docker-compose up -d mongo
```

#### CORS ошибки в браузере
Убедитесь что `NEXT_PUBLIC_API_BASE` указывает на правильный URL бэкенда.

#### MTProto: "Not configured"
Проверьте что в `.env` бота есть:
```env
TELEGRAM_API_ID=...
TELEGRAM_API_HASH=...
```
И перезапустите бот.

---

## 📊 API Endpoints

### Bot API (http://localhost:8080/api)

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/health` | GET | Проверка здоровья |
| `/bots` | GET/POST | Управление ботами |
| `/bots/validate` | POST | Проверка токена |
| `/bots/:id` | GET/PUT/DELETE | Операции с ботом |
| `/posts` | GET/POST | Управление постами |
| `/posts/:id` | GET/PUT/DELETE | Операции с постом |
| `/channels/info` | POST | Информация о канале |
| `/mtproto/status` | GET | Статус MTProto |
| `/mtproto/auth/start` | POST | Начало авторизации |
| `/mtproto/auth/complete` | POST | Завершение авторизации |
| `/mtproto/channels/parse` | POST | Парсинг канала |

### Примеры запросов

**Проверка токена бота:**
```bash
curl -X POST http://localhost:8080/api/bots/validate \
  -H "Content-Type: application/json" \
  -d '{"token": "7123456789:AAH..."}'
```

**Создание поста:**
```bash
curl -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{
    "botId": "...",
    "authorId": "...",
    "content": "Привет мир!",
    "status": "published",
    "publishTarget": "channel"
  }'
```

---

## 🔒 Безопасность

⚠️ **Для локальной разработки только!**

В продакшене:
- Используйте надёжный `JWT_SECRET`
- Храните токены зашифрованными
- Используйте HTTPS
- Настройте CORS для конкретных доменов
- Не коммитьте `.env` файлы

---

## 📱 Тестирование Telegram интеграции

### Создание тестового бота

1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### Создание тестового канала

1. Создайте канал в Telegram
2. Сделайте его публичным (`@test_channel`)
3. Добавьте бота как админа
4. Дайте права на публикацию

### Тестирование webhook (продакшен)

Для продакшена нужен публичный URL. Варианты:

**ngrok:**
```bash
ngrok http 8080
# Скопируйте URL и установите webhook
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://xxx.ngrok.io/api/webhook/telegram"
```

**Cloudflare Tunnel:**
```bash
cloudflared tunnel --url http://localhost:8080
```

---

## 🚢 Деплой

### Bot → Railway

```bash
cd packages/bot
npm install -g @railway/cli
railway login
railway init
railway up
```

### Admin → Vercel

```bash
cd packages/admin
npm install -g vercel
vercel login
vercel --prod
```

Настройте переменную `NEXT_PUBLIC_API_BASE` на URL Railway.

---

## 📞 Поддержка

- GitHub Issues: [voicekeeper/issues](https://github.com/hpnssflw/voicekeeper/issues)
- Telegram: @hpnssflw

